package DAO;

import org.hibernate.Session;

public class Student_DAO_Imp {
	
	@Override
	public List<Student> getStudents(){
		Session currentSession=sessionFactory.getCurrentSession();
		
		Query<Student> query=currentSession.createQuery("frpm student",Student.class);
	}

}
